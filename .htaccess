RewriteEngine On

# Only rewrite if it's not a real file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# page-name → ?page=page-name
RewriteRule ^([0-9a-zA-Z_-]+)/?$ index.php?page=$1 [NC,L]